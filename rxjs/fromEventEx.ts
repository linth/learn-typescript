/**
 * fromEvent example.
 * 
 */

import { fromEvent } from "rxjs";


// const button = document.getElementById('myButton');

// fromEvent(button, 'click')
// 	.subscribe(() => console.log(`按鈕被點擊了!`))